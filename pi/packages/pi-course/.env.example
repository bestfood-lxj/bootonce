# Optional manual OpenAI-compatible transport configuration.
# Copy to .env locally; the offline workshop tests do not read these values.
OPENAI_COMPATIBLE_BASE_URL=https://api.openai.com/v1
OPENAI_COMPATIBLE_API_KEY=
OPENAI_COMPATIBLE_MODEL=
